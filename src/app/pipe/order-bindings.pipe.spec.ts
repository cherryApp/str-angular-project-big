import { OrderBindingsPipe } from './order-bindings.pipe';

describe('OrderBindingsPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderBindingsPipe();
    expect(pipe).toBeTruthy();
  });
});
