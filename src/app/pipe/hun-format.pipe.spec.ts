import { HunFormatPipe } from './hun-format.pipe';

describe('HunFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new HunFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
